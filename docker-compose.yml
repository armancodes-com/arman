version: '3'

services:
  nextjs-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: armancodes/nextjs-app:latest
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./path/to/your/app:/app
