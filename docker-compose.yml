services:
  nextapp:
    container_name: armancodes-com
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - nginx_apps_network

networks:
  nginx_apps_network:
    external: true
    name: nginx_apps_network
